export type SkillType = {
  title: string;
  icon?: JSX.Element;
};

export type IconTextPropType = {
  institution: string;
  icon?: JSX.Element;
  certificate: string;
  certificateAddress: string;
}
